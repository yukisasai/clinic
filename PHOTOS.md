# 写真差し替え指示書 — 01-clinic

**このLPの完成度は、この作業をやるかどうかで9割決まります。**
配色・書体・余白はすでに詰めてあります。残っているのは写真だけです。

現状はすべて SVG プレースホルダー（`./images/*.svg`）です。
**同じファイル名・同じ比率の `.webp` を置き、`index.html` 内の `.svg` を `.webp` に置換**すれば入れ替わります。

```bash
# 一括置換
sed -i '' 's/\.svg"/\.webp"/g' index.html   # macOS
```

---

## 入手先（すべて商用利用可・クレジット不要）

| サイト | 向いているもの | 注意 |
|---|---|---|
| **Unsplash** | 人物・空間・質感。最も"素材集っぽくない" | 英語検索のほうが良い写真が出る |
| **Pexels** | 医療・施術シーン | 日本語検索も可 |
| **写真AC** | 日本人モデル | 無料会員はDL数制限あり。医療系は数が少ない |

> **日本人モデルが必要な枠**（hero / doctor / ba-*）は、Unsplash/Pexels だと欧米系ばかり出ます。
> ここだけは写真ACか、有料の PIXTA / Adobe Stock を検討してください。
> **顔が出る枠で欧米系モデルを使うと、日本の医院LPとしては一気に嘘くさくなります。**

---

## 枠ごとの指定

### `hero.webp` — 1400 × 1750（4:5 縦）★最重要
- **被写体**: 30〜40代日本人女性。**カメラ目線にしない。** 半身または横顔、視線は斜め下。
- **表情**: 微笑まない。落ち着いた無表情〜わずかな安堵。美容医療の客は「悩んで来ている」ので、満面の笑みは共感を切る。
- **光**: 自然光・順光。影を作らない。白〜ベージュの背景。
- **構図**: 顔を**画面の上1/3**に置く。下側はテキストが乗って隠れるので、余白のある写真を選ぶ。
- 検索語: `japanese woman skincare natural light` / 写真AC「40代 女性 肌」
- ❌ 避ける: 白衣の医師とガッチリ握手／ビフォーアフターの合成／過度なレタッチのツヤ肌

### `ba-01-before/after.webp`, `ba-02-*.webp` — 各 900 × 900（正方形）★スライダーの核
- **絶対条件**: before と after は**同じ人物・同じ角度・同じ明るさ**であること。
  スライダーで重ねるため、ズレていると即座にバレます。ここが一番の落とし穴です。
- 実案件では医院提供の症例写真を使う枠。ポートフォリオでは:
  - 同一人物の写真1枚を用意し、**after 側だけ**を軽くレタッチ（色ムラを薄く）して2枚にする
  - フッターに「サンプル画像」と明記する
- 顔全体ではなく**頬まわりのクローズアップ**にすると、素材の制約を受けにくく説得力も上がります。

### `doctor.webp` — 900 × 1200（3:4 縦）
- 白衣の日本人女性医師、腕組みをしない、正面やや斜め。背景は診察室かグレー無地。
- 検索語: `japanese female doctor portrait` / 写真AC「女性 医師 白衣」

### `clinic-01.webp` — 1000 × 750（4:3）
- 待合または診察室。**人を入れない。** 明るく、木目か白基調。
- 検索語: `minimal clinic interior waiting room`

### 未使用だが用意済みの枠
`clinic-02` / `device` / `flow-01〜04` — セクションを増やすときに使えます。今回のHTMLでは未参照です。

---

## 書き出し設定

```bash
# ImageMagick で一括変換（品質82・メタ削除）
for f in *.jpg; do
  magick "$f" -strip -quality 82 -resize 1400x "${f%.jpg}.webp"
done
```

- **hero だけは `loading="lazy"` を付けない**（すでに `fetchpriority="high"` 指定済み）
- 目標: hero 200KB以下、その他 各100KB以下
- LCP が 2.5秒を超えると、それだけでCVRが落ちます。ここはケチらず圧縮してください。

---

## 差し替え後チェック

- [ ] before/after スライダーを動かして、2枚の位置がズレていない
- [ ] hero の顔が、スマホ幅でテキストに隠れていない
- [ ] 顔出し枠のモデルが日本人で統一されている
- [ ] Lighthouse の LCP が 2.5秒以内
- [ ] 全画像に意味のある alt が入っている（装飾目的なら `alt=""`）
